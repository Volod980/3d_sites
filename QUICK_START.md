# 🚀 Швидкий запуск

## Для користувачів (без технічних знань)

### Windows

1. **Переконайтесь що встановлено Node.js**
   - Завантажте з https://nodejs.org/ (версія 18 або новіша)
   - Встановіть з налаштуваннями за замовчуванням

2. **Завантажте проект**
   - Скачайте весь проект собі на комп'ютер
   - Розпакуйте в будь-яку папку (наприклад `C:\tour-builder`)

3. **Запустіть сайт**
   - Знайдіть файл `start.bat` в папці проекту
   - Двічі клацніть по ньому
   - Почекайте 10-15 секунд поки все запуститься

4. **Відкрийте браузер**
   - Браузер має автоматично відкритись
   - Якщо ні, перейдіть на http://localhost:5173

### macOS / Linux

1. **Переконайтесь що встановлено Node.js**
   ```bash
   node --version  # Має показати v18 або вище
   ```
   Якщо немає, встановіть з https://nodejs.org/

2. **Перейдіть в папку проекту**
   ```bash
   cd /шлях/до/3d_sites
   ```

3. **Запустіть сайт**
   ```bash
   ./start.sh
   ```

4. **Відкрийте браузер**
   - Перейдіть на http://localhost:5173

---

## Для розробників

### Варіант 1: Автоматичний запуск (рекомендується)

**Linux/Mac:**
```bash
./start.sh
```

**Windows:**
```
start.bat
```

### Варіант 2: Ручний запуск

**Крок 1: Встановлення залежностей**
```bash
# Backend
cd backend
npm install
cd ..

# Frontend
cd frontend
npm install
cd ..
```

**Крок 2: Запуск в режимі розробки**

Відкрийте 2 термінали:

**Термінал 1 (Backend):**
```bash
cd backend
npm run dev
```

**Термінал 2 (Frontend):**
```bash
cd frontend
npm run dev
```

**Крок 3: Відкрийте браузер**
```
http://localhost:5173
```

---

## 🎯 Як користуватись

1. **Введіть місто** в поле пошуку (Київ, Львів, Одеса, Харків...)
2. **Натисніть "Знайти пам'ятки"** і зачекайте 5-10 секунд
3. **Оберіть місця** які хочете відвідати (клік на картках або маркерах)
4. **Побудуйте маршрут** натиснувши кнопку (потрібно мінімум 2 місця)
5. **Перегляньте** оптимальний маршрут на карті!

---

## 🛑 Як зупинити

**Автоматичний запуск:**
- Linux/Mac: Натисніть `Ctrl+C` в терміналі
- Windows: Закрийте вікна Backend та Frontend

**Ручний запуск:**
- Натисніть `Ctrl+C` в обох терміналах

---

## ❗ Troubleshooting

### Порт зайнятий
Якщо бачите помилку "Port 3000 already in use" або "Port 5173 already in use":

**Linux/Mac:**
```bash
# Знайти процес
lsof -i :3000
lsof -i :5173

# Вбити процес
kill -9 <PID>
```

**Windows:**
```bash
# Знайти процес
netstat -ano | findstr :3000
netstat -ano | findstr :5173

# Вбити процес
taskkill /PID <PID> /F
```

### Немає Node.js
Встановіть Node.js з https://nodejs.org/ (версія 18 або новіша)

### Помилки при npm install
Спробуйте:
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

---

## 📞 Підтримка

Якщо щось не працює:
1. Переконайтесь що Node.js встановлено (`node --version`)
2. Перевірте чи порти 3000 і 5173 вільні
3. Перевірте чи є інтернет (потрібен для OpenStreetMap API)
4. Перезапустіть сервери
